name: CI Pipeline Test

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Simulate Workload
        run: |
          echo "Running a test pipeline..."
          sleep 5

      - name: Finished
        run: echo "Workflow complete!"
